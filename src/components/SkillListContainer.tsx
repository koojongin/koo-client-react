import React from 'react';
import styled from 'styled-components';

const StyledSkillListContainer = styled.div``;

export default function SkillListContainer() {
  return <StyledSkillListContainer>스킬목록 예정</StyledSkillListContainer>;
}
